**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

E**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

C**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

/**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

/**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

C**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

:**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

T**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

;**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

F**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

:**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

:**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

"**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

C**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

j**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

"**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

:**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

N**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

'**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

B**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

P**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

N**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

G**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

L**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

Y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

S**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

N**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

E**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

V**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

E**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

R**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

T**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

<**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

T**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

>**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

I**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

C**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

N**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

D**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

S**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

q**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

#**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

T**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

B**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

q**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

L**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

Y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

S**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

;**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

N**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

E**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

V**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

E**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

R**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

L**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

Y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

S**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

T**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

S**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

[**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

]**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

N**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

I**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

N**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

-**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

`**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

"**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

x**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

"**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

T**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

L**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

Y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

S**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

I**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

—**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

L**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

W**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

Y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

S**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

p**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

,**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

u**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

E**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

A**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

b**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

k**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

T**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

y**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

I**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

***Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

:**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

c**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

w**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

v**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

s**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

h**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

l**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

d**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

i**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

o**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

 **Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

f**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

r**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

a**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

g**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

m**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

e**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

n**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

t**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

.**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.


**Try to write a near-miss every time; include it only if it is real.** The
attempt is what has value — failing to find a near-compliance case tells you the
rule is vague, or obvious enough to need no guardrail. Fix the rule then.

**Never fabricate one to fill the slot.** A weak example outweighs the
instruction above it just as a good one does, and teaches the wrong edge. Leave
it out where it does not fit or would not make the policy better — that needs
no note and is not a gap.

